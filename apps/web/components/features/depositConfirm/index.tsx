"use client";
import Default from "components/layouts/default";
import DepositConfirm from "components/modules/depositConfirm";

export default function DepositConfirmPage(): JSX.Element {
  return (
    <Default>
      <DepositConfirm />
    </Default>
  );
}
