import Default from "components/layouts/default";
import NewProposal from "components/modules/proposals/new";

export default function DaoPage(): JSX.Element {
  return (
    <Default>
      <NewProposal />
    </Default>
  );
}
